void main() {
  //  const filePath = './test/helloworld.wasm';
  // Uint8List byteCode = Uint8List.fromList(File(filePath).readAsBytesSync());
  // print('byte length is ${byteCode.length}');
  // final response =
  //     await wallet.executeSC(address, byteCode, dataStore: null);
  // print(response);
  // Slot start = Slot(period: 101357, thread: 15);
  // Slot end = Slot(period: 101370, thread: 15);
  // EventFilter ef = EventFilter(start: start, end: end);
  // List<Event>? ev = await api.getFilteredSCOutputEvent(ef);
  // print(ev![0].encode());
}
